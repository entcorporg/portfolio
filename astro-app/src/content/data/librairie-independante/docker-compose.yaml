services:
  apache:
    image: httpd:alpine
    container_name: librairie-independante
    ports:
      - "3000:80"
    volumes:
      - ./:/usr/local/apache2/htdocs:ro
      - ./apache.conf:/usr/local/apache2/conf/httpd.conf:ro
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    driver: bridge
